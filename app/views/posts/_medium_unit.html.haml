%a.post_wrapper{class: :medium, href: post_url(post)}
  .post{style: "background-image: url('#{post.image.url(:medium)}')"}
    .background
      .titles
        %h2= post.title
        %h3
          In:
          = post.category.name
        %p= post.summary
